CREATE TABLE TB_FOCUS_SESSION (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    USER_ID NUMBER NOT NULL,
    START_TIME TIMESTAMP,
    END_TIME TIMESTAMP,
    AVG_BPM NUMBER,       
    AVG_NOISE_DB NUMBER,  
    STATUS VARCHAR2(20)  
);

